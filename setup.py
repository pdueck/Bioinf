from setuptools import setup

setup(name='gc_content',
      version='0.1',
      description='A script that prints id, GC% from sequence records of a fasta file to stdout.',
      url='https://github.com/pdueck/Bioinf.git',
      author='Pierre Dueck',
      author_email='dykmnis@gmail.com',
      license='MIT',
      packages=['Bioinf'],
      zip_safe=False)
